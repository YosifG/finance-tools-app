# Payments App project
An app with a mock implementation of two tools for SMBs- an invoice and payment link generator. Built using microservices architecture.

Run using **docker-compose up --build**.

Technologies used:
Flask
SQLAlchemy + SQLite
Lambda
SNS
Pytest